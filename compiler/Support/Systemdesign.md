Layers - lexer, parsing, semantic passes, asm

Errors/syntax errors will be added to a queue <Diagnostics>

When an error has be encountered, the error will silently fail until a layer has
completed its pass


